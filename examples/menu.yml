# Menu Example
# The "menu.yml" file is the default that will be loaded if no filename
# is provided.
menu:
  hello: echo The message is %{message}
  hi: echo hi %{name}

args:
  name:
  - Harry
  - Lloyd
