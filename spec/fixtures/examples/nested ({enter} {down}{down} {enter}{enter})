> (Use ↑/↓ arrow keys, press Enter to select, and letter keys to filter)
> docker
  git> docker
> (Use ↑/↓ arrow keys, press Enter to select, and letter keys to filter)
> images
  containers
  stack> 
  images
> containers
  stack> 
  images
  containers
> stack> stack
> (Use ↑/↓ arrow keys, press Enter to select, and letter keys to filter)
> deploy
  list> deploy
> stubbed: docker stack deploy -c docker-compose.yml mystack
